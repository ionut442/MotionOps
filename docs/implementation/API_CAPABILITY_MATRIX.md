# MotionOps API Capability Matrix

| API area | Operation | Status | Evidence | Risks | Decision | Related tasks |
|---|---|---|---|---|---|---|
| Manifest | `documentAccess: "dynamic-page"` | Documentation only | Official Figma manifest docs: `https://developers.figma.com/docs/plugins/manifest/`; dynamic-loading docs: `https://developers.figma.com/docs/plugins/migrating-to-dynamic-loading/`; manifest configured locally. | Runtime plugin load not yet tested in Figma. | Required for new plugins; use async-first architecture. | P0-004 |
| Plugin UI | `figma.showUI` and `figma.ui.postMessage` | Documentation only + automated message smoke | Official Figma UI docs: `https://developers.figma.com/docs/plugins/creating-ui/`; Playwright tests browser-side message handling. | Browser smoke does not prove Figma host delivery. | Use typed bridge and validate messages at both boundaries. | P0-B01, P0-B02 |
| Plugin UI | `figma.ui.onmessage` | Documentation only + unit validation | Official Figma onmessage docs: `https://developers.figma.com/docs/plugins/api/properties/figma-ui-onmessage/`; message type guard tests. | Runtime handler not yet exercised inside Figma. | Reject malformed UI payloads and return typed error. | P0-B01 |
| Undo | `figma.commitUndo()` | Live verified with warnings | Installed `@figma/plugin-typings` 1.130.0 and official Figma `commitUndo` docs: `https://developers.figma.com/docs/plugins/api/properties/figma-commitundo/`; P0-010 live evidence `p010-mriz*`; `docs/verification/live-figma/P0-010/RESULTS.md`. | One-write manual transactions require Strategy C (`commitUndo() -> write`). Style-involved U03/U05 undo restored original but one native Redo did not restore applied state. Separate U06 actions require two native Undo steps. | Product undo grouping may use the proven case-specific orderings, must re-read after Undo/Redo, and must not assume style redo restoration. | P0-010 |
| Motion | Read `animations` | Live verified for P0-006 fixtures | Official Figma Motion docs state Motion API is Beta: `https://developers.figma.com/docs/plugins/api/Motion/`; durable schema-v2 live run `p006-mrin2zvh-ac0f05`; `docs/verification/live-figma/P0-006/EVIDENCE_INVENTORY.md`. | API shape may change; write behavior remains unverified. | Preserve unknown fields and evaluate capability by schema-v2 target provenance, not canvas selection. | P0-006 |
| Motion | Read `manualKeyframeTracks` | Live verified for P0-006 fixtures | Official Figma Motion docs: `https://developers.figma.com/docs/plugins/api/Motion/`; node property docs: `https://developers.figma.com/docs/plugins/api/properties/nodes-manualkeyframetracks/`; durable schema-v2 live run `p006-mrin2zvh-ac0f05`. | Manual-track replacement, ID preservation, and write safety remain unverified. | Preserve unknown fields; do not implement replacement writer until P0-007 verifies it. | P0-006, P0-007 |
| Motion | Read `animationStyles` | Live verified for P0-006 and P0-008 fixtures | Official Figma Motion docs: `https://developers.figma.com/docs/plugins/api/Motion/`; node property docs: `https://developers.figma.com/docs/plugins/api/properties/nodes-animationstyles/`; durable schema-v2 live run `p006-mrin2zvh-ac0f05`; style-instance run `p008-mriu1fap-25c85d`. | P0-008 proves direct reapply duplicates style instances for the selected style. | Keep style data separate from manual tracks; style mutation must use applied-instance remove/reapply plus re-read checks. | P0-006, P0-008 |
| Motion | Read/write `timelines` | Supported with warning | Official Figma Motion docs; installed `@figma/plugin-typings` 1.130.0 exposes `setTimelineDuration(id: string, duration: number): void`; durable schema-v2 read run `p006-mrin2zvh-ac0f05`; timeline-duration run `p009-mrivc1j7-56e68c`. | Figma accepts a duration below the final keyframe without clamping; derived animation `timelineDuration` mirrors the shorter duration. Product code must decide whether below-final-keyframe writes are allowed. | Timeline duration writes may be used only with explicit target provenance, immediate re-read, max-keyframe policy, restoration/rollback plan, and no undo grouping assumptions. | P0-006, P0-009 |
| Motion | Replace manual tracks preserving IDs | Investigation complete; safe subset with limitations | Official docs: `https://developers.figma.com/docs/plugins/api/properties/nodes-applymanualkeyframetrack/`; Motion docs: `https://developers.figma.com/docs/plugins/api/Motion/`; installed `@figma/plugin-typings` 1.130.0; retained schema-1 run `p007-mriq1s7l-2b2f3c`. | W02 regenerated an edited keyframe ID; W04 re-read `LINEAR` easing with an extra cubic-bezier field. Strict keyframe-ID preservation is disproven for timing edits. | Manual full-track replacement is feasible for tested shapes only with mandatory re-read/remap; never assume edited keyframe IDs survive; compare easing semantically and preserve raw readback. | P0-007 |
| Motion | Style-instance update/reapply | Partial support; direct reapply duplicates | Installed `@figma/plugin-typings` 1.130.0 exposes `figma.motion.figmaAnimationStyles`, `animationStyles`, `applyAnimationStyle`, and `removeAnimationStyle`; retained run `p008-mriu1fap-25c85d` produced terminal S01-S06 evidence with readable fixture instances. | S01-S05 direct reapply/update paths duplicated applied instances; S06 remove/reapply ended with one instance. | Do not implement update-in-place with direct `applyAnimationStyle`. Use remove applied instance ID, reapply available application ID, re-read, and reject duplicates. | P0-008 |
| Motion | Component and instance writes | Harness present; retained full-run evidence rejected locally | `src/shared/p011Registry.ts`, `src/shared/p011Evidence.ts`, `src/plugin/diagnostics/p011Fixtures.ts`, `tests/p011Evidence.test.ts`, lab UI P0-011 runner, retained `test-results/p011-*`. | Local full-run manifests are not accepted because at least one case ended ERROR; accepted retained manifests are single-case runs. | Do not productize component/instance writes from rejected full-run evidence. Audit a newer accepted manifest before marking P0-011 DONE. Component-property keyframe tracks remain P0-012. | P0-011 |
| Motion | Component-property tracks and property writes | Live verified with conclusive mixed behavior | `src/shared/p012Registry.ts`, `src/shared/p012Evidence.ts`, `src/plugin/diagnostics/p012Fixtures.ts`, `tests/p012Evidence.test.ts`, `tests/p012Cp09Harness.test.mjs`, lab UI P0-012 runner, `test-results/p012-mrj6qxvi-217610*`. | BOOLEAN component-property writes are supported and isolated through `setProperties`; component-property definitions/state are property-API-only for CP01/CP02; CP08 records unsupported/not-exposed property kinds. CP09 shows plugin-triggered Undo does not restore readable semantic state after the supported write. | Require explicit write probe before production use. Treat unsupported/read-only outcomes as valid capability findings. Treat Undo for component-property writes as supported-with-warning/partial and re-read after rollback attempts. | P0-012 |
| Production adapter | Normalized Motion snapshot and verified write primitives | Implemented for accepted Phase 0 scope | `src/plugin/motion/`, `tests/motionAdapter.test.ts`, `docs/verification/PHASE-01.md`. | Adapter represents accepted limitations but does not run new live Figma evidence. Undo orchestration, multi-node operations, and product UI remain future work. | All future Motion features must use the normalized adapter boundary. Raw unknown beta fields may leave the adapter only in explicit diagnostics/unsupported representations. | P1-011 |
| Production verification | Expected/actual post-write diff reports | Implemented for accepted Phase 0 scope | `src/plugin/motion/expectations.ts`, `src/plugin/motion/semantic-equality.ts`, `src/plugin/motion/diff.ts`, `src/plugin/motion/verification.ts`, `tests/motionVerification.test.ts`, `docs/verification/PHASE-01.md`. | Verification is limited to normalized production data and accepted write primitives. Unsupported component-property Motion-track writes and unsafe direct style reapply do not verify as success. Unknown beta fields may be unverifiable when not safely comparable. | Use projection expectations and fresh normalized actual snapshots. Treat keyframe IDs and applied style-instance IDs as observational, CP09 success as verified-with-warning, and capability downgrade as an explicit verification difference. | P1-012 |
| Production stale protection | Pre-write semantic state guards | Implemented for accepted Phase 0/P1 write primitives | `src/plugin/motion/fingerprint.ts`, `src/plugin/motion/stale-detection.ts`, `tests/motionStaleDetection.test.ts`, `docs/verification/PHASE-01.md`. | Stale checking does not apply writes, rescan, rebase, batch operations, or prompt users. Direct style reapply remains unsafe unless future evidence changes the capability. Unknown relevant beta data may be unverifiable. | Capture operation-specific normalized guards at preview time, freshly re-read before apply, compare semantic fingerprints, block stale/unverifiable state, then use P1-012 expected/actual verification after any future write. Snapshot IDs are diagnostic only. | P1-013 |
| Dynamic page | Large-file scanning and cancellation | Unknown | Official docs require dynamic-page compatibility. | Broad scans can be slow or invalid without page loading strategy. | Await harness and fixture evidence. | P0-014 |
| Motion API Lab | Diagnostic command bridge | Automated harness only; Figma blank-UI regression fixed | `MOTION_DIAGNOSTIC_REQUEST`/`MOTION_DIAGNOSTIC_RESULT` validated by unit tests and Playwright lab smoke; `build:lab` now inlines UI assets for `figma.showUI(__html__)`. | Does not prove Figma Motion API success. | Use for Phase 0 evidence collection only after rebuilding the development plugin. | P0-005 |
| Motion API Lab | `applyAnimationStyle` method detection | Method exposed and live exercised | Installed typings: `applyAnimationStyle(styleId, animationStyleData?): string`; retained run `p008-mriu1fap-25c85d`. | Applying selected application ID `Scale` completed and re-read, but direct reapply duplicated. | Treat method presence as insufficient; product writes need duplicate checks. | P0-008 |
| Motion API Lab | `removeAnimationStyle` method detection | Method exposed and live exercised | Installed typings: `removeAnimationStyle(id): void`; retained run `p008-mriu1fap-25c85d`. | S06 removed an applied `AnimationPresetId:*` instance and re-applied `Scale`, ending with one style instance. | Remove/reapply is the only verified style mutation pattern. | P0-008 |
| Motion API Lab | `applyManualKeyframeTrack` method detection | Live method execution observed, strict contract blocked | Official docs: `https://developers.figma.com/docs/plugins/api/properties/nodes-applymanualkeyframetrack/`; retained run `p007-mriq1s7l-2b2f3c` executed W01-W08 fixture replacements. | Execution without exception is not proof that IDs are preserved; W02 regenerated a keyframe ID. | Treat P0-007 as blocked until exact keyframe ID preservation is proven or the product contract changes. | P0-007 |
| Motion API Lab | `removeManualKeyframeTrack` method detection | Awaiting live Figma verification | Official docs: `https://developers.figma.com/docs/plugins/api/properties/nodes-removemanualkeyframetrack/`; harness reports method presence where exposed. | Method presence is not proof that removal is safe. | Use fixture-only write tests in P0-007. | P0-007 |
| Motion API Lab | `setTimelineDuration` method detection | Live exercised | Installed typings: `setTimelineDuration(id: string, duration: number): void`; retained run `p009-mrivc1j7-56e68c` records T01-T06 duration, identity, isolation, normalization, and restoration evidence. | Emitted manifest accepted=false due the initial harness treating derived duration readback as unrelated mutation; audited behavior is conclusive. | Use exact seconds duration writes with re-read and policy checks; do not infer undo grouping. | P0-009 |
| Motion API Lab | P0-006 generated fixture runner | Live verified and retained | `src/shared/p006Registry.ts`, `src/plugin/diagnostics/targetResolver.ts`, `src/plugin/diagnostics/p006Fixtures.ts`, `scripts/motion-evidence-collector.mjs`, `playwright.config.ts`, durable run `p006-mrin2zvh-ac0f05`. | Generated fixture creation uses write APIs only as development infrastructure; outputs are not formal P0-007+ write proof. | Use schema v2 `target.resolvedNodeIds` and `diagnostic.nodesReadCount`; ignore canvas selection as proof of read success; keep Playwright output in `.playwright-test-results`. | P0-006 |
| Motion API Lab | P0-007 replacement runner | Live Figma run retained but not accepted | `src/shared/p007Registry.ts`, `src/shared/p007Evidence.ts`, `src/plugin/diagnostics/p007Fixtures.ts`, `tests/p007Evidence.test.ts`, `scripts/motion-evidence-collector.mjs`, `test-results/p007-mriq1s7l-2b2f3c.manifest.json`. | Manifest accepted=false because W02 and W04 failed strict evidence checks. | Keep output separate as schema 1 P0-007 evidence; do not infer product write support from tests or Playwright. | P0-007 |
| Motion API Lab | P0-008 style-instance runner | DONE with conclusive partial behavior | `src/shared/p008Registry.ts`, `src/shared/p008Evidence.ts`, `src/plugin/diagnostics/p008Fixtures.ts`, `tests/p008Evidence.test.ts`, `tests/p008Runner.test.mjs`, `scripts/motion-evidence-collector.mjs`, `test-results/p008-mriu1fap-25c85d*`. | Runner emitted S01-S06 evidence; S01-S05 duplicated, S06 remove/reapply passed. | Keep output separate as schema 1 P0-008 evidence; do not start P0-009 from an assumption of style update-in-place support. | P0-008 |
| Motion API Lab | P0-012 component-property track runner | DONE with conclusive mixed behavior | `src/shared/p012Registry.ts`, `src/shared/p012Evidence.ts`, `src/plugin/diagnostics/p012Fixtures.ts`, `tests/p012Evidence.test.ts`, `tests/p012Cp09Harness.test.mjs`, `scripts/motion-evidence-collector.mjs`, `test-results/p012-mrj6qxvi-217610*`. | CP09 focused manifest accepted=true. BOOLEAN component-property write is supported through the shared CP07/CP09 writer, but plugin-triggered Undo invalidates target/sibling re-reads instead of restoring semantic state. CP01/CP02 are READ_ONLY and CP08 is UNSUPPORTED/not-exposed. | Use component-property writes only after explicit write probe. Treat `triggerUndo()` for this path as supported-with-warning/partial; re-read and handle stale target nodes. Do not treat CP01/CP02/CP08 negative findings as defects. | P0-012 |
| Manifest | `networkAccess.devAllowedDomains` for collector | Documentation plus local config | Official Figma manifest/network docs; `manifest.json` has `allowedDomains: ["none"]` and `devAllowedDomains: ["http://localhost:3847"]`. | Collector must never be required by production UI. | Keep localhost access development-only. | P0-006A |
| Undo | `figma.triggerUndo()` | Live verified with warning | Installed `@figma/plugin-typings` 1.130.0 exposes `triggerUndo(): void`; official Figma API index says it reverts to the last `commitUndo()` state; U08 live evidence `p010-mrizx0su-b6764f-U08-partial-failure-rollback.json`. | The retained U08 manifest was emitted by an older fatal-error rule, but the evidence is conclusive: `triggerUndoSafe=true`, `partialWritesRemained=false`, and `redoRestoredApplied=true`. | Use `triggerUndo()` as rollback for controlled partial failure paths, then re-read before continuing. | P0-010 |
